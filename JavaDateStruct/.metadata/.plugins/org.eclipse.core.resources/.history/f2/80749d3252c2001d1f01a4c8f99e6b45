package kmp;

import java.util.Iterator;

/**
 * 实现KMP算法
 * @author LykanHyper
 *
 */
public class KMP_Alogorithm {
	private void maim() {
		// TODO Auto-generated method stub
		

	}
	public void nextCreat(String s2) {
		char[] charArray = s2.toCharArray();
		int[] next = new int[s2.length()];
		//j为前缀末尾位置，i为后缀末尾位置
		int j = 0;
		next[0] = 0;
		for(int i = 1;i < next.length;i++) {
			
		}
	}
}
